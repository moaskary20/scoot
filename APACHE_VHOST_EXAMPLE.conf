# مثال على إعداد Apache Virtual Host مع Reverb
# انسخ هذا في /etc/apache2/sites-available/linerscoot.com.conf

<VirtualHost *:80>
    ServerName linerscoot.com
    ServerAlias www.linerscoot.com
    Redirect permanent / https://linerscoot.com/
</VirtualHost>

<VirtualHost *:443>
    ServerName linerscoot.com
    ServerAlias www.linerscoot.com

    # SSL Configuration
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/linerscoot.com/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/linerscoot.com/privkey.pem
    Include /etc/letsencrypt/options-ssl-apache.conf
    SSLCertificateChainFile /etc/letsencrypt/options-ssl-apache.conf

    # WebSocket Proxy for Reverb
    RewriteEngine on
    RewriteCond %{HTTP:Upgrade} websocket [NC]
    RewriteCond %{HTTP:Connection} upgrade [NC]
    RewriteRule ^/app/(.*)$ ws://127.0.0.1:8080/app/$1 [P,L]

    # HTTP Proxy (fallback)
    ProxyPass /app/ http://127.0.0.1:8080/app/
    ProxyPassReverse /app/ http://127.0.0.1:8080/app/

    # Laravel Application
    DocumentRoot /var/www/scoot/public
    
    <Directory /var/www/scoot/public>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # Logs
    ErrorLog ${APACHE_LOG_DIR}/linerscoot_error.log
    CustomLog ${APACHE_LOG_DIR}/linerscoot_access.log combined

    # Security Headers
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
</VirtualHost>

