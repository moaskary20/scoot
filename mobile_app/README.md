# LinerScoot Mobile App

تطبيق موبايل لتأجير السكوترات الذكية - Flutter

## المميزات

- 🔐 تسجيل الدخول والتسجيل
- 🗺️ عرض السكوترات على الخريطة
- 📍 تتبع الموقع في الوقت الفعلي
- 🚀 بدء وإكمال الرحلات
- 💰 إدارة المحفظة والاشتراكات
- 📱 واجهة مستخدم عربية (RTL)

## البنية الأساسية

```
lib/
├── core/
│   ├── constants/      # الثوابت (API, App)
│   ├── models/         # النماذج
│   ├── services/       # الخدمات (API, WebSocket)
│   └── utils/          # الأدوات المساعدة
├── features/
│   ├── auth/           # المصادقة
│   ├── home/           # الصفحة الرئيسية
│   ├── map/            # الخريطة
│   ├── trips/          # الرحلات
│   ├── wallet/         # المحفظة
│   └── profile/        # الملف الشخصي
└── shared/
    ├── theme/          # الثيم
    └── widgets/        # الويدجت المشتركة
```

## البدء

### المتطلبات

- Flutter SDK 3.10.4 أو أحدث
- Dart SDK 3.10.4 أو أحدث
- Android Studio / Xcode (للتطوير)

### التثبيت

```bash
# تثبيت الحزم
flutter pub get

# تشغيل التطبيق
flutter run
```

### الإعدادات

1. قم بتحديث `lib/core/constants/api_constants.dart` مع رابط الـ API الخاص بك
2. قم بتحديث `lib/core/constants/app_constants.dart` حسب احتياجاتك

## الحزم المستخدمة

- `http` / `dio` - للاتصال بالـ API
- `provider` - لإدارة الحالة
- `geolocator` / `google_maps_flutter` - للموقع والخريطة
- `shared_preferences` - للتخزين المحلي
- `web_socket_channel` - للاتصال المباشر

## التطوير

### إضافة شاشة جديدة

1. أنشئ مجلد في `lib/features/[feature_name]/screens/`
2. أضف الشاشة الجديدة
3. أضف الـ route في `main.dart`

### إضافة خدمة جديدة

1. أنشئ ملف في `lib/core/services/`
2. أضف الـ service class
3. استخدمه في الـ providers

## الترخيص

جميع الحقوق محفوظة © 2025 LinerScoot
